/**
 * This code defines a function called calculateStatModifier that takes a number as an argument. It subtracts 10 from the argument and then divides the result by 2. The function returns the calculated value.
 */
const calculateStatModifier = (statValue: number) => (statValue - 10) / 2;
export default calculateStatModifier;
